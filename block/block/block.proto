syntax = "proto3";

package block;
option go_package="./block";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Block {
  rpc Ping(Request) returns(Response);
}
